<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MacroTrack" parent="android:Theme.Material.Light.NoActionBar">
        <!-- We use Compose for all UI, but we still need a base theme for the Activity -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>
</resources>
